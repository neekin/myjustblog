extends ../layout


block style
  include ../includes/style
block foot
  include ../blocks/foot
block content
   .container.content
     .row.page 
       h2.title #{article.title}
             span.subtitle #{article.subtitle}
       p.publish-time(title="#{ moment(article.publishtime).format('MMM D YYYY')}") #{moment(article.publishtime).format('MMM D YYYY')}
       .textbody !{article.articlebody}
     .aboutme.row
      .col-sm-9
       h3.pull-left 关于我
       .clear
       p 90年后 码农
      .col-sm-3
        h3.pull-right 分享这篇文章
        .clear
